# github-actions-workshop

## TO-DO: Levantar un servidor web

- Crear un endpoint para cada operacion:
    - POST -> suma, resta, multiplicación
- Agregar prueba del endpoint en tests/
- Desplegarse con Docker en el CI -> CD